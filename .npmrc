# Netlify Functions Node.js version
NODE_VERSION=18

# Use legacy peer deps to avoid dependency conflicts
legacy-peer-deps=true

# Fix for Rollup optional dependencies issue on Netlify
optional=true
fund=false
audit=false